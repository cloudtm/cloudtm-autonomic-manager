/**
 * This package provides classes that 
 * are used for embedding List data within a Measurement.
 * <p>
 * A List is defined like so:
 * <pre>
 * MList mList = new DefaultMList(ProbeAttributeType.INTEGER);
 * </pre>
 * which defines the type of the elements in the list.
 * Values are added to the list like so:
 * <pre>
 * mList.add(1).add(2).add(3);
 * </pre>
 */
package eu.reservoir.monitoring.core.list;
